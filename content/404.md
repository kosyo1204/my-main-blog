---
title: ページが見つかりません
layout: layouts/base
permalink: /404.html
eleventyExcludeFromCollections: true
templateEngine: njk
---

<div class="error-page">
  <div class="error-content">
    <div class="error-illustration">
      <span class="error-code">404</span>
      <span class="error-emoji">🔍</span>
    </div>
    <h1 class="error-title">ページが見つかりません</h1>
    <p class="error-description">
      お探しのページは存在しないか、移動した可能性があります。
    </p>
    <div class="error-actions">
      <a href="{{ '/' | url }}" class="btn btn-primary">
        🏠 ホームに戻る
      </a>
      <button class="btn btn-secondary" onclick="history.back()">
        ← 前のページに戻る
      </button>
    </div>
    {%- if collections.articles and collections.articles.length > 0 %}
    <div class="error-suggestions">
      <h2>こちらの記事はいかがですか？</h2>
      <div class="article-cards">
        {%- for article in collections.articles | reverse %}
        {%- if loop.index0 < 3 %}
        <article class="article-card">
          <h3>
            <a href="{{ article.url | url }}">{{ article.data.title }}</a>
          </h3>
          {%- if article.data.publishedAt %}
          <p class="article-meta">
            <time datetime="{{ article.data.publishedAt | dateISO }}">
              {{ article.data.publishedAt | dateISO }}
            </time>
          </p>
          {%- endif %}
        </article>
        {%- endif %}
        {%- endfor %}
      </div>
    </div>
    {%- endif %}
  </div>
</div>

